import 'package:flutter_test/flutter_test.dart';

// Widget tests will be added once RustLib can be initialised in a test harness.
void main() {
  testWidgets('smoke test placeholder', (tester) async {
    expect(true, isTrue);
  });
}
